# Copier en .env.local pour le dev local (.env.local est ignore par git).
# En production sur Vercel, definir cette variable dans Settings > Environment Variables.

# URL de l'API backend
# Dev local : http://localhost:5000
# Prod : https://ton-backend.onrender.com (ou autre)
VITE_API_URL=http://localhost:5000
